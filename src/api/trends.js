function getInterestOverTime(request, response) {
  response.json({'omg': 'hai'});
}

function getDummy(request, response) {
  response.json({'omg': 'hai'});
}

module.exports = {
  getInterestOverTime,
  getDummy
};
